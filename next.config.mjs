/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["img.clerk.com","ktceocdocbuhokgehohd.supabase.co"],
       // Add other domains if needed
    },
  };
  
 export default nextConfig
  